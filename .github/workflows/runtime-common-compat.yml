name: Control Plane Runtime Common Compatibility
run-name: Control Plane Runtime Common Compat / ${{ github.event_name }} / ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      runtime_common_ref:
        description: Runtime-common ref or SHA to validate
        required: false
        default: main
        type: string
  repository_dispatch:
    types:
      - runtime_common_released

permissions:
  contents: read

concurrency:
  group: control-plane-runtime-common-compat-${{ github.ref }}
  cancel-in-progress: true

jobs:
  runtime-common-compat:
    name: runtime-common-compat
    runs-on: ubuntu-22.04
    env:
      TEST_MODE: "1"
      LOG_FORMAT: "TEXT"
      LOG_LEVEL: "INFO"
      AZURE_STORAGE_ACCOUNT_NAME: localdev
      AZURE_STORAGE_CONNECTION_STRING: DefaultEndpointsProtocol=https;AccountName=test;AccountKey=key;EndpointSuffix=core.windows.net
      AZURE_CONTAINER_COMMON: common
      AZURE_CONTAINER_BRONZE: bronze
      AZURE_CONTAINER_SILVER: silver
      AZURE_CONTAINER_GOLD: gold
      AZURE_CONTAINER_PLATINUM: platinum
      AZURE_FOLDER_MARKET: market-data
      AZURE_FOLDER_FINANCE: finance-data
      AZURE_FOLDER_EARNINGS: earnings-data
      AZURE_FOLDER_TARGETS: price-target-data
      ALPHA_VANTAGE_API_KEY: test
      MASSIVE_API_KEY: test
      POSTGRES_DSN: postgresql://user:pass@localhost:5432/asset_allocation?sslmode=disable
      DISPATCH_RUNTIME_COMMON_SHA: ${{ github.event.client_payload.runtime_common_sha }}
      DISPATCH_RUNTIME_COMMON_REF: ${{ github.event.client_payload.runtime_common_ref }}
    steps:
      - name: Resolve runtime-common ref
        id: runtime-common
        shell: bash
        run: |
          set -euo pipefail
          ref="${{ inputs.runtime_common_ref }}"
          if [ "${GITHUB_EVENT_NAME}" = "repository_dispatch" ]; then
            ref="${DISPATCH_RUNTIME_COMMON_SHA:-${DISPATCH_RUNTIME_COMMON_REF}}"
          fi
          ref="${ref:-main}"
          echo "ref=${ref}" >> "$GITHUB_OUTPUT"

      - name: Checkout control-plane repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: asset-allocation-control-plane

      - name: Checkout runtime-common repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: ${{ format('{0}/asset-allocation-runtime-common', github.repository_owner) }}
          ref: ${{ steps.runtime-common.outputs.ref }}
          path: asset-allocation-runtime-common

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.14"

      - name: Configure optional pip indexes
        shell: bash
        run: |
          set -euo pipefail
          if [ -n "${{ secrets.PYTHON_PACKAGE_INDEX_URL }}" ] || [ -n "${{ secrets.PYTHON_PACKAGE_EXTRA_INDEX_URL }}" ]; then
            cat > "${RUNNER_TEMP}/pip.conf" <<EOF
          [global]
          EOF
            if [ -n "${{ secrets.PYTHON_PACKAGE_INDEX_URL }}" ]; then
              echo "index-url = ${{ secrets.PYTHON_PACKAGE_INDEX_URL }}" >> "${RUNNER_TEMP}/pip.conf"
            fi
            if [ -n "${{ secrets.PYTHON_PACKAGE_EXTRA_INDEX_URL }}" ]; then
              echo "extra-index-url = ${{ secrets.PYTHON_PACKAGE_EXTRA_INDEX_URL }}" >> "${RUNNER_TEMP}/pip.conf"
            fi
            echo "PIP_CONFIG_FILE=${RUNNER_TEMP}/pip.conf" >> "$GITHUB_ENV"
          fi

      - name: Install compatibility dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -r asset-allocation-control-plane/requirements.lock.txt
          python -m pip install -r asset-allocation-control-plane/requirements-dev.lock.txt
          python - <<'PY' > asset-allocation-control-plane/shared-python-deps.txt
          import tomllib
          from pathlib import Path

          pyproject = tomllib.loads(Path("asset-allocation-control-plane/pyproject.toml").read_text(encoding="utf-8"))
          for dependency in pyproject["project"]["dependencies"]:
              if dependency.startswith("asset-allocation-") and not dependency.startswith("asset-allocation-runtime-common=="):
                  print(dependency)
          PY
          python -m pip install -r asset-allocation-control-plane/shared-python-deps.txt
          python -m pip install -e asset-allocation-runtime-common/python
          python -m pip install -e asset-allocation-control-plane --no-deps

      - name: Run compatibility suite
        shell: bash
        run: |
          set -euo pipefail
          cd asset-allocation-control-plane
          python -m pytest tests/test_multirepo_dependency_contract.py tests/api/test_internal_endpoints.py -q

      - name: Write compatibility summary
        shell: bash
        run: |
          {
            echo "## Control-plane runtime-common compatibility"
            echo ""
            echo "- Runtime-common ref: \`${{ steps.runtime-common.outputs.ref }}\`"
            echo "- Result: passed"
          } >> "${GITHUB_STEP_SUMMARY}"
